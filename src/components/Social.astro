<style is:global>
  .social {
    width: calc(var(--s1) * 3);
  }
  .social a {
    color: var(--color-foreground);
    display: block;
    padding: var(--s1);
    outline: 0.0625rem solid transparent;
    outline-offset: -0.125rem;
    transition: fill 0.1s ease-in-out 0s;
    background: none;
  }
  .social a:focus {
    color: var(--color-foreground);
    outline: 0.0625rem solid var(--color-foreground);
    border-radius: 0.5rem;
  }
  .social a svg {
    width: 1.5rem;
    fill: var(--color-foreground);
  }
  .social a svg:hover {
    fill: var(--color-incidental);
  }
</style>

<div class="social"><slot /></div>
