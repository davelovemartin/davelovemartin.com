---
interface Props {
  href: string;
}

const { href } = Astro.props;
---

<style is:global>
  a.card:link,
  a.card:visited,
  a.card:hover,
  a.card:active {
    padding-left: calc(var(--s1) / 2) !important;
    padding-right: calc(var(--s1) / 2) !important;
    padding-top: calc((var(--s1) / 2) - 0.125rem) !important;
    padding-bottom: calc((var(--s1) / 2) + 0.125rem) !important;
  }
  a.card:link,
  a.card:visited,
  a.card:active {
    color: var(--color-foreground);
    border: 2px solid var(--color-foreground);
    background: none !important;
    box-shadow: var(--shadow1);
    transition: all var(--t2) ease-out;
    border-radius: var(--border-radius);
    text-decoration: none !important;
    margin-block-start: 0;
    margin-block-end: 0;
  }
  .card:hover {
    background: none;
    box-shadow: var(--shadow2);
    transform: scale(1.01236);
  }
  .card div h2 {
    min-height: calc(3 * var(--s1));
  }
</style>

<a class="card" href={href}>
  <slot />
</a>
